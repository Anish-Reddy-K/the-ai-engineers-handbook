---
title: "Neural Networks"
slug: "neural-networks"
weight: 6
draft: true
---

Perceptrons and multi-layer networks. Weights, biases, and activation functions (ReLU, Sigmoid, Softmax). **Learning process**: loss functions, gradient descent, backpropagation. Epochs and batch sizes. Overfitting and regularization. CNNs for vision, RNNs for sequences.
